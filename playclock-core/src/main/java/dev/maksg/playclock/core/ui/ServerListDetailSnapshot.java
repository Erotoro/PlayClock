package dev.maksg.playclock.core.ui;

public record ServerListDetailSnapshot(
        String titleText,
        String todayText,
        String totalText,
        String sessionText,
        String lastPlayedText) {
}
